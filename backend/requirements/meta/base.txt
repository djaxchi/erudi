# numpy 2.x: pulled in by the mlx-vlm swap (opencv/datasets/py3langid 0.3.x).
# The whole pinned stack supports numpy 2; src only touches numpy via .tolist().
numpy==2.4.6
psutil==7.0.0
py-cpuinfo==9.0.0
huggingface_hub==1.10.1
pydantic==2.11.5
sentence-transformers==5.4.0
# transformers: normalized to 5.10.2 for ALL platforms. Now possible because
# torch-CUDA was dropped — every build runs a CPU/recent torch (>=2.7). The value
# is dictated by mlx-vlm 0.6.2 on mac (5.12 changed AutoProcessor resolution and
# breaks Gemma3 -> "Unrecognized processing class"); 5.10.2 is the known-good.
# 5.10.2 needs torch>=2.7 at import (references torch.float8_e8m0fnu).
transformers==5.10.2
python-dotenv==1.1.0
pypdf==5.6.0
tqdm==4.67.1
# fastapi/starlette bumped by the mlx-vlm swap: mlx-vlm requires starlette>=1
# (a 0.x->1.x major), which pulls a compatible fastapi. Validated against the
# full backend suite (423 passed). uvicorn unchanged.
fastapi==0.136.3
starlette==1.2.1
uvicorn==0.34.0
sqlalchemy==2.0.40
requests==2.32.3

# ===== LangChain agent stack (conversation/arena refactor — PR1) =====
# Pinned to the versions resolved on first install. Transitive deps
# (langchain-core, langgraph-checkpoint, openai, tiktoken, ormsgpack)
# resolve automatically. See requirements/README.md.
langchain==1.3.2
langchain-openai==1.2.2
langgraph==1.2.2

# ===== PostgreSQL / pgvector stack (Postgres migration + KB refactor) =====
# Python 3.12 REQUIRED: pgserver ships binary wheels for cp39-cp312 only
# (no cp313 wheel, no sdist). CI already runs 3.12.
# Pinned to the versions resolved + probed on first install (P0).
pgserver==0.1.4
psycopg[binary]==3.3.4
# Schema migrations (#96): forward-only Alembic revisions, auto-applied at startup
# (lifespan) against the persistent embedded cluster. Compatible with SQLAlchemy 2.0.
alembic==1.18.4
langchain-postgres==0.0.17
langgraph-checkpoint-postgres==3.1.0
langchain-text-splitters==1.1.2
python-docx==1.2.0
openpyxl==3.1.5
# ===== RAG answer quality (PR3, issue #81) =====
# Language detection for the dynamic answer-language instruction in the KB
# prompt. 0.3.0 declares numpy>=2.0.0, aligned with the numpy 2.x bump the
# mlx-vlm swap brings in; same langid.MODEL_FILE/LanguageIdentifier API as 0.2.2.
py3langid==0.3.0
