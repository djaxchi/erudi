# Windows-only runtime deps. Currently none: hardware detection is cross-platform
# (CPU/CUDA engines = psutil + stdlib winreg for the CPU brand, CUDA_Engine = pynvml),
# so `wmi` was unused and removed. Kept as a placeholder for the entrypoints that
# `-r` this file.
